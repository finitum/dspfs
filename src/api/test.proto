syntax = "proto3";

package dspfs.test;


service TestService {
  rpc test (TestMessage) returns (Empty) {}
}

message TestMessage {
  repeated int32 test = 1;
}

// Todo: Why does google.protobuf.Empty not work?
message Empty {

}
